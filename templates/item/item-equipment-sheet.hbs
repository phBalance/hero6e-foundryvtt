{{log 'HEROSYS-EQUIPMENT ITEM' this}}
<form class="{{cssClass}}" autocomplete="off" data-id="{{item._id}}" data-realId="{{item.system.realId}}">
    <header class="sheet-header flexrow">
        <img class="profile" name="img" src="{{item.img}}" title="{{item.name}}" data-edit="img" />

        <div class="header-details flexrow">
            <h1 class="charname">
                <input name="system.ALIAS" type="text" value="{{item.system.ALIAS}}" placeholder="Equipment ALIAS" />
            </h1>
        </div>
    </header>

    {{!-- Sheet Body --}}
    <section class="sheet-body">
        <div class="form-group">
            <label>Carried</label>
            <input type="checkbox" name="system.active" {{checked system.active}} data-dtype="Boolean" />
        </div>

        <div class="form-group">
            <label>Weight</label>
            <input type="text" name="system.WEIGHT" value="{{system.WEIGHT}}" data-dtype="Number" />
        </div>

        <div class="form-group">
            <label>Price</label>
            <input type="text" name="system.PRICE" value="{{system.PRICE}}" data-dtype="Number" />
        </div>

        {{!-- Common --}}
        {{> (getModulePath 'item/item-partial-common.hbs') }}

        {{!-- Attack --}}
        {{!-- {{#if (eq item.system.subType 'attack') }}
            {{> (getModulePath 'item/item-attack-partial.hbs') item=item }}
        {{/if}} --}}

        {{!-- Active Points --}}
        {{> (getModulePath 'item/item-partial-active-points.hbs') }}

        {{!-- Adders & Modifiers --}}
        {{> (getModulePath 'item/item-partial-adders-modifiers.hbs') }}
    </section>

</form>