{{ log 'HEROSYS apply-aid-card' this }}
<div class="hero chat-card item-card apply-sense-affeccting-card" data-actor-id="{{actor._id}}" data-item-id="{{item._id}}" {{#if
  tokenId}}data-token-id="{{tokenId}}" {{/if}}>

  <header class="card-header flexrow">
    <img src="{{item.img}}" title="{{item.name}}" width="36" height="36" />
    <h3>vs {{targetToken.name}}</h3>
  </header>

  <div>
    Attacker: <span class="item-name">{{item.actor.name}}</span>
  </div>
  <div>
    Attack: <span class="item-name">{{{itemName item}}}</span>
  </div>

  <div class="card-section">
    <div class="description-tiny">Defense: {{ defense }}</div>
  </div>

  {{item.system.XMLID}}: {{targetToken.actor.name}} for {{ damageData.bodydamage }} segments.



</div>