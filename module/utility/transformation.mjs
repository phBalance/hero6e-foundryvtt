export async function performTransformation(
    item,
    thisTransformationDamage,
    defenseDescription,
    effectsDescription,
    isFade,
    targetActor,
) {
    console.log("performTransformation", {item, thisTransformationDamage, defenseDescription, effectsDescription, isFade, targetActor});
    return null;
}

export async function renderTransformationChatCards(cardOrCards, transformationItemTags, defenseTags) {
    console.log("renderTransformationChatCards", {cardOrCards, transformationItemTags, defenseTags});
}